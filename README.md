arch-run is a tool to run ephemeral execution of applications of arch linux repository, so you can run applications without installing them on your system. It is similar to `nix run` but for arch linux.

```bash
arch-run neofetch
```
